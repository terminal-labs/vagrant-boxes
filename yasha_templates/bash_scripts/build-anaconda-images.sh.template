mkdir -p build

{% for os in ['tl-ubuntu-1604-64bit-2tb'] %}
		cp -r box-creation/full-image-dependencies {{ os }}-anaconda/.full-image-dependencies

		mv {{ os }}-anaconda/Vagrantfile {{ os }}-anaconda/Vagrantfile.old
		cp {{ os }}/Vagrantfile {{ os }}-anaconda/Vagrantfile
		cp build/{{ os }}.box {{ os }}-anaconda/{{ os }}.box
		cd {{ os }}-anaconda
		vagrant up
		vagrant ssh -c "cd /vagrant/.full-image-dependencies;sudo bash install-packages.sh"
		vagrant package --output ../build/{{ os }}-anaconda.box
		vagrant destroy -f
		rm -rf .full-image-dependencies
		rm -rf .vagrant
		cd -
		rm {{ os }}-anaconda/Vagrantfile.old
{% endfor %}
